#see https://github.com/moby/moby/issues/1916

version: '2.1'
services:
  debootstrap:
    build:
      context: ./debootstrap
    # needed for bind mounting proc inside the container
    privileged: true
    entrypoint: /root/init-lb.sh
